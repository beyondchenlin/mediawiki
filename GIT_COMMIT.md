# <type>(<scope>): <subject>
# │       │         │
# │       │         └─⫸ 简短描述（不超过 50 字符）
# │       └─⫸ 影响范围：janvas|workbench-web|canvas|run|gateway|orchestrator|infra|docs|plan|i18n 等（不要用 sprint/phase 作为 scope）
# └─⫸ 类型：feat|fix|perf|refactor|style|docs|test|build|ci|chore

# 📝 正文（详细说明）─────────────────────────────────────────
#
# 用户提问：
# <用户的原话提问，保留关键上下文>
#
# 问题描述：
# <AI 对问题的理解和分析>
#
# 问题根因：（如果是 fix）
# <分析问题的技术根本原因>
#
# 解决方案：
# 1. <具体实现步骤 1>
# 2. <具体实现步骤 2>
# 3. <具体实现步骤 3>
#
# 改动范围：
# • <改动的文件或模块 1>
# • <改动的文件或模块 2>
#
# 测试验证：
# ✓ <测试用例 1>
# ✓ <测试用例 2>
#
# ─────────────────────────────────────────────────────────────
#
# 🤖 Generated with [Claude Code](https://claude.com/claude-code)
#
# Co-Authored-By: Claude <noreply@anthropic.com>

# ═══════════════════════════════════════════════════════════════
# 📋 Type 类型说明
# ═══════════════════════════════════════════════════════════════
# feat:     新功能（feature）
# fix:      Bug 修复
# perf:     性能优化（performance）
# refactor: 代码重构（不改变功能）
# style:    代码格式调整（不影响代码逻辑）
# docs:     文档更新
# test:     测试相关
# build:    构建系统或依赖变动
# ci:       CI 配置变动
# chore:    其他杂项
#
# ═══════════════════════════════════════════════════════════════
# ✅ 提交规范（4 大黄金法则）
# ═══════════════════════════════════════════════════════════════
# 1. 用人能看懂的话，说明改动内容
# 2. 提交记录开头有分类标签
# 3. 每个功能，只有一条提交记录
# 4. 每条提交记录，只对应一个功能
#
# ═══════════════════════════════════════════════════════════════
# 🔄 团队协作（PR）
# ═══════════════════════════════════════════════════════════════
# - PR 流程/合并策略：CLAUDE.md（Section 9）
# - PR 描述模板：.github/PULL_REQUEST_TEMPLATE.md
#
# ═══════════════════════════════════════════════════════════════
# 📖 参考文档：CLAUDE.md（Section 9）
# 💡 使用 slash command：输入 /commit 获取智能生成提交信息
# ═══════════════════════════════════════════════════════════════

# 配置 Git 用户信息（首次使用）
# git config --global user.name "beyond.chenlin"
# git config --global user.email "beyond.chenlin@gmail.com"

# ═══════════════════════════════════════════════════════════════
# 🔤 编码规范（避免 GitHub Squash 出现 ????）
# ═══════════════════════════════════════════════════════════════
# 现象：GitHub "Squash and merge" 页面里 commit 正文显示为 ????
# 原因：在 Windows PowerShell 5.1 中通过管道把中文喂给 git（例如 `... | git commit -F -`），
#       默认输出编码可能不是 UTF-8，导致中文在写入前就被替换成 '?'。
# 团队规则（按优先级）：
# 1) Commit 标题/正文允许且推荐中文，但必须让 `git commit` 打开编辑器填写（确保保存为 UTF-8）
# 2) 禁止在 PowerShell 里用管道/重定向把中文喂给 git（例如 `... | git commit -F -`）
# 3) 如果不得不用 PowerShell（不推荐），先执行：
#      $OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
